export enum DeviceType {
    StreamDeck,
    StreamDeckMini,
    StreamDeckXL,
    StreamDeckMobile,
    CorsairGKeys
}

export enum TargetType {
    BOTH,
    HARDWARE,
    SOFTWARE
}

export enum StateType {
    ON,
    OFF
}

export enum EventType {
    ALL,
    PI,
    PLUGIN,
    NONE
}
